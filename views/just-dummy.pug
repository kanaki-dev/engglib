section(class="sm:col-span-5 lg:col-span-6 bg-gray-800 p-4")
    <table class="md:w-full md:text-sm md:text-left md:rtl:text-right text-gray-500 md:dark:text-gray-400">
        <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
            <tr>
                <th scope="col" class="px-6 py-3"> Year </th>
                <th scope="col" class="px-6 py-3"> Name </th>
                <th scope="col" class="px-6 py-3"> View </th>
            </tr>
        </thead>
        <tbody>
            <tr class="bg-white border-b dark:bg-gray-800 dark:border-gray-700">
                <td class="px-6 py-4">Laptop</td>
                th(
                    scope="row"
                    class="px-6 py-4 font-medium text-gray-900 whitespace-nowrap dark:text-white"
                ) Apple MacBook Pro 17"
                td(class="px-6 py-4")
                    a(href="#" class="text-blue-600") View
            </tr>
        </tbody>
    </table>
</section>

//- 
section(class="sm:col-span-5 lg:col-span-6 bg-gray-800 p-4")
            if (!data && !sub)
                div(class="flex justify-center items-center h-[100%]")
                    h1(class="font-bold text-2xl") Content well be shown here
            else if (!data && sub)
                div(class="")
                    each val in sub 
                        +subName(val)
            else 
                each val in data
                    +tableContent(val)